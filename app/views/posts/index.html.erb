<%= render "layouts/headerAll" %>

<h3 class='welcome'> Welcome <%=@user.name%> </h3>

<div class='messages' >
  <% @posts.each do |post| %>
    <%= link_to user_post_path(@user, post) do %>
      <%= "#{post.message} - #{post.created_at.to_date}" %>
    <% end %>
    </p>
  <% end %>
</div>