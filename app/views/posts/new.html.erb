<%= render "layouts/headerUser" %>

<div class="row">
  <%= form_for([@user, @post]) do |form| %>
    <p><%= form.label :message %></p>
    <%= form.text_area :message%>
    <%= form.submit "Submit" %>
  <% end %>
</div>
